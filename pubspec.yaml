name: finance_tracker
description: Розумний трекер витрат + AI-фінансовий помічник для українців

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  shared_preferences: ^2.2.0
  intl: ^0.19.0
  uuid: ^4.0.0
  http: ^1.1.0
  fl_chart: ^0.65.0
  provider: ^6.0.0
  freezed_annotation: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^3.0.0
  build_runner: ^2.4.0
  freezed: ^2.4.1

flutter:
  uses-material-design: true
  assets:
    - assets/